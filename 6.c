#include<stdio.h>
#include<conio.h>
void main()
{
  int year;
  printf("enter the year");
  scanf("%d",year);
  if(year%d==0)
   {
    if(year%d==0)
    {
      printf("leap year");
      }
    else
     {
       printf("not ieap year");
       }
     }
      else
      {
        pritf(" leap year")
        }
        getch();
        }
